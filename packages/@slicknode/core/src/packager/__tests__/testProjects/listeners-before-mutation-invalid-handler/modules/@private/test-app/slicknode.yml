module:
  id: '@private/test-app'
  label: Private Module
  namespace: Test

runtime:
  engine: nodejs@8
  memory: 256

listeners:
  - event: mutation.createUser.BEFORE
    handler: ./invalidrelativepath
