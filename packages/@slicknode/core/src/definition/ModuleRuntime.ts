/**
 * Created by Ivo Meißner on 24.01.18.
 *
 */

import { RuntimeConfig } from '@slicknode/runtime-executor';

export type ModuleRuntime = {
  /**
   * The runtime config of the module
   */
  config: RuntimeConfig;
};
